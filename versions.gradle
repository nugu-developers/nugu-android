def getVersionNameFromCode(code) {
    Integer major = code / 10000
    Integer minor = (code % 10000) / 100
    Integer patch = code % 100

    return "$major.$minor.$patch"
}

ext {
    compileSdkVersion = 33
    minSdkVersion = 19
    targetSdkVersion = 33

    // 3rd
    androidMaterialVersion = "1.4.0"
    androidXAppcompatVersion = "1.3.0"
    glideVersion = "4.12.0"
    gsonVersion = "2.8.6"
    okhttp3Version = "4.9.3"
    androidXBrowserVersion = "1.4.0"
    koinVersion = "2.2.3"
    kotlinxCoroutinesAndroidVersion = "1.5.0"
    exoPlayerVersion = "2.16.1"
    lottiVersion = "3.7.0"
    jsonVersion = "20180813"
    javaxAnnotationApiVersion = "1.2"

    // test
    junitVersion = "4.13.2"
    testJunitKtxVersion = "1.1.3"
    testCoreKtxVersion = "1.4.0"
    mokitoCoreVersion = "3.8.0"
    mokitoInlineVersion = "3.8.0"
    mokitoKotlinVersion = "3.1.0"
    androidXJunitVersion = "1.1.3"
    androidXEspressoCoreVersion = "3.4.0"
    robolectricVersion = "4.3.1"

    // SDK
    defaultKeensenseVersion = "0.3.0"
    defaultJademarbleVersion = "0.2.7"
    defaultSilvertrayVersion = "4.4.1"

    defaultBaseNuguVersionCode = 10711
    defaultBaseNuguVersionName = "1.7.11"

    keensenseVersion = rootProject.hasProperty("keensenseVersion") ? rootProject.keensenseVersion : defaultKeensenseVersion
    jademarbleVersion = rootProject.hasProperty("jademarbleVersion") ? rootProject.jademarbleVersion : defaultJademarbleVersion
    silvertrayVersion = rootProject.hasProperty("silvertrayVersion") ? rootProject.silvertrayVersion : defaultSilvertrayVersion

    nuguVersionCode = rootProject.hasProperty("nuguVersionCode") ? rootProject.nuguVersionCode : defaultBaseNuguVersionCode
    nuguVersionName = rootProject.hasProperty("nuguVersionName") ? rootProject.nuguVersionName : defaultBaseNuguVersionName

    println("====================================================")
    println("configurations (nugu-andrid/versions.gradle)")
    println("====================================================")
    println("rootDir.path: ${rootDir.path}")
    println("rootProject.name: ${rootProject.name}")
    println("##nuguVersionCode: $nuguVersionCode")
    println("##nuguVersionName: $nuguVersionName")
    println("##isReleaseMode: $isReleaseMode")
    println("##publishSnapshot: $publishSnapshot")
    println("##isSubmodule: ${rootProject.hasProperty("sdkAsSubmodule")}")
    println("====================================================")
}